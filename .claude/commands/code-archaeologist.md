# /code-archaeologist — Codebasis erkunden und dokumentieren

Delegiert an den Subagent `code-archaeologist` (`.claude/agents/code-archaeologist.md`).

## Anweisungen

$ARGUMENTS

Starte den Subagent **code-archaeologist**, um die übergebene Codebasis bzw. das Verzeichnis (Standard: `services/`) zu analysieren und einen Bericht zu Architektur, Risiken und priorisierten Maßnahmen zu erstellen.
