# /security-expert — Sicherheits- und Datenschutz-Audit

Delegiert an den Subagent `security-expert` (`.claude/agents/security-expert.md`).

## Anweisungen

$ARGUMENTS

Starte den Subagent **security-expert** für ein Sicherheits-Audit: Umgang mit Secrets (API-Keys, Umgebungsvariablen), schützenswerte Interviewdaten und DSGVO-Konformität der Dienste unter `services/`. Read-only — keine Änderungen vornehmen.
