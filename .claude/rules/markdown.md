---
paths:
  - "**/*.md"
---

# Markdown Rule

- Read `.claudedocs/agent-system.md` before editing agent documentation.
- Keep canonical rules in `.claudedocs/`; adapter files should link instead of duplicating full rules.
- Use relative repository paths.
- Run `python3 scripts/check-agent-docs.py` after agent-documentation changes.
