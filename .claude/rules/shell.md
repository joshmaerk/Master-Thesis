---
paths:
  - "scripts/**/*.sh"
---

# Shell Rule

- Read `.claudedocs/latex-workflow.md` and `.claudedocs/quality-checks.md` before changing build/setup scripts.
- Use Bash with `set -euo pipefail` for project scripts.
- Quote variables and avoid destructive commands.
- Document new scripts in `README.md`.
