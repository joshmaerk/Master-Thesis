---
paths:
  - ".github/workflows/**/*.yml"
  - ".github/workflows/**/*.yaml"
  - "**/*.json"
---

# YAML and JSON Rule

- Read `.claudedocs/quality-checks.md` before CI or configuration changes.
- Keep external-write workflows explicit and documented.
- Do not add secrets to committed configuration files.
