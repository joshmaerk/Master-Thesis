# services/.env — Alle API-Schlüssel für die Thesis-Services
# Kopiere diese Datei nach services/.env und trage deine Werte ein.
# Die .env-Datei wird NICHT in Git eingecheckt (siehe .gitignore).

# ── Zotero Sync ───────────────────────────────────────────────────────────────

# Privater Zotero-API-Schlüssel
# Erstellen: Zotero → Bearbeiten → Einstellungen → Feeds/API → Neuen privaten Schlüssel erstellen
# Benötigte Rechte: Bibliothek lesen (read)
ZOTERO_API_KEY=dein_api_schluessel_hier

# Numerische Zotero-User-ID
# Zu finden unter: Zotero → Einstellungen → Feeds/API → „Ihre User-ID für API-Aufrufe"
ZOTERO_USER_ID=1234567

# Bibliothekstyp: "user" (persönliche Bibliothek, Standard) oder "group" (Gruppenbibl.)
ZOTERO_LIBRARY_TYPE=user

# Gruppen-ID – nur erforderlich wenn ZOTERO_LIBRARY_TYPE=group
# ZOTERO_GROUP_ID=9876543

# ── Literature Review ─────────────────────────────────────────────────────────

# Anthropic API-Schlüssel (für Journal- und Autoren-Bewertung via Claude)
# Erstellen: console.anthropic.com → API Keys → Create Key
ANTHROPIC_API_KEY=sk-ant-api03-...

# GitHub Personal Access Token (für Issue-Erstellung)
# Erstellen: github.com → Settings → Developer settings → Personal access tokens → Fine-grained
# Benötigte Rechte: Issues (read & write) für dieses Repository
GITHUB_TOKEN=ghp_...

# GitHub Repository im Format owner/repo
GITHUB_REPOSITORY=joshmaerk/Master-Thesis

# ── Transkriptions-Service ────────────────────────────────────────────────────

# OpenAI API-Schlüssel (für Whisper Speech-to-Text)
# Erstellen: platform.openai.com → API Keys → Create new secret key
# Kosten: ca. 0,006 USD/Minute Audio (Whisper-1 Modell)
OPENAI_API_KEY=sk-...

# Claude-Modell für Dresing & Pehl Formatierung (optional)
# Standard: claude-opus-4-8 (beste Qualität)
# Alternativen: claude-haiku-4-5-20251001 (günstiger, schneller)
# ANTHROPIC_MODEL=claude-opus-4-8
